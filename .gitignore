# ไฟล์ที่สร้างขึ้นเองในแต่ละเครื่อง ไม่ต้อง sync ผ่าน git
# (ถ้า push ไฟล์พวกนี้ขึ้นไป จะทับ/ชนกันมั่วเวลาหลายคนใช้ร่วมกัน)
#
# ข้อยกเว้น: model.pkl ต้อง commit ด้วย เพราะเว็บที่ deploy บนคลาวด์
# ไม่มีทางรัน 3_train_model.py เองได้ ต้องมีโมเดลที่เทรนไว้แล้วติดไปกับโค้ดเลย

handspeak.db
speech.mp3

__pycache__/
*.pyc

.claude/
