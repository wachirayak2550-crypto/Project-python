# HandSpeak — ระบบ AI ล่ามภาษามือเป็นข้อความ

> ไฟล์นี้คือ "แผนที่โปรเจค" สำหรับ Claude Code
> วิธีใช้: เปิดโปรเจคนี้ใน Claude Code แล้วพิมพ์ว่า "อ่าน brief.md แล้วเริ่มทำ STEP 1"
> ทำทีละ STEP เท่านั้น อย่าข้าม อย่าทำรวดเดียวทั้งหมด

---

## 1. โปรเจคนี้คืออะไร

เว็บแอปที่เปิดกล้องโน้ตบุ๊ค อ่าน **ท่าสะกดนิ้วมือภาษาไทย (แบบท่านิ่ง)** จากมือผู้ใช้
แล้ว **แสดงเป็นข้อความบนจอ** ว่าเป็นตัวอะไร ทำหน้าที่เหมือนล่ามภาษามือแบบง่าย

**สโคปที่จะทำในโปรเจคนี้ (เฟสแรก):**
- **ตัวเลข 0-9** (10 ตัว — ท่านิ่งล้วน)
- **พยัญชนะไทยที่เป็นท่านิ่ง และท่าต่างกันชัด** (~15 ตัว)
- **รวมประมาณ 25 ตัว**

ตัวอักษรที่ต้องขยับมือ (dynamic) และสระ = **ไม่ทำในเฟสนี้** (เก็บเป็นการต่อยอด)

## 2. ข้อจำกัดสำคัญ (ต้องเคารพทุกข้อ)

- คนทำเป็น **นักศึกษาปี 1** พื้นฐาน Python แค่ระดับ for loop / while loop / function
- **ไม่มี GPU** ต้องรันบนโน้ตบุ๊คธรรมดาได้
- **งบ = 0 บาท** ใช้ของฟรี opensource เท่านั้น
- มีเวลา **ประมาณ 2 สัปดาห์**
- เป้าหมาย: **ส่งผ่าน + demo ทำงานได้จริงต่อหน้าอาจารย์**
- โค้ดต้อง **อ่านง่าย มีคอมเมนต์ภาษาไทยอธิบาย** เพราะเจ้าของต้องเข้าใจเพื่อตอบตอน demo

## 3. วิธีการทางเทคนิค (สำคัญ — ห้ามใช้วิธีอื่น)

**ห้ามใช้การเทรนภาพแบบ CNN/Deep Learning** เพราะกิน GPU กินเวลา และยากเกินไปสำหรับปี 1

ให้ใช้แนวทางนี้:
```
กล้อง → MediaPipe จับจุดมือ 21 จุด (แต่ละจุดมี x,y,z = 63 ตัวเลขต่อมือ)
      → เอา 63 ตัวเลขนี้ไปให้โมเดล scikit-learn ทาย
      → ทายได้ว่าเป็นตัวอะไร → แสดงเป็นข้อความตัวใหญ่บนจอ
```
หัวใจ: แปลงมือเป็น "ชุดตัวเลข" แล้วสอนโมเดลว่า "ตัวเลขชุดนี้ = ตัว ก"
เป็นการเทียบตัวเลข ไม่ใช่ประมวลผลภาพหนักๆ → เทรนเสร็จในไม่กี่วินาที รันบน CPU ได้สบาย

### เรื่อง 2 มือ (สำคัญ — อ่านให้ดี)
- การสะกดนิ้วมือไทยใช้ **มือเดียว** แต่เราจะตั้งค่าให้ระบบ **มองเห็นและวาดจุดได้สูงสุด 2 มือ** (เผื่อผู้ใช้ใช้มือซ้ายหรือขวา และดูสมบูรณ์ตอน demo)
- **แต่ตอนป้อนให้โมเดลทาย ให้ใช้แค่มือเดียว** (มือที่เด่นสุด/ใหญ่สุดในเฟรม) = 63 ตัวเลขคงที่เสมอ
- เหตุผล: ถ้าป้อน 2 มือบ้าง 1 มือบ้าง จำนวนตัวเลขจะไม่คงที่ โมเดลจะพัง → ล็อคที่มือเดียวเพื่อความเสถียร

## 4. เครื่องมือที่ใช้ (ฟรีทั้งหมด)

- `opencv-python` — เปิดกล้อง อ่านภาพ
- `mediapipe` — จับจุดมือ 21 จุด (ตั้งค่า max_num_hands = 2)
- `scikit-learn` — โมเดลทาย (ใช้ **RandomForest** พอ ไม่ต้องซับซ้อน)
- `numpy` — จัดการตัวเลข
- `streamlit` — ทำหน้าเว็บ + เปิดกล้องในเบราว์เซอร์ (สำหรับ demo)

## 5. โครงสร้างไฟล์ที่ต้องการ

```
handspeak/
├── brief.md              ← ไฟล์นี้
├── requirements.txt      ← รายชื่อ library ที่ต้องติดตั้ง
├── labels.py             ← กำหนดรายชื่อตัวที่จะทำ (เลข + พยัญชนะ)
├── 1_test_camera.py      ← STEP 1: ทดสอบกล้อง + จับ 2 มือขึ้นจอ
├── 2_collect_data.py     ← STEP 2: อัดข้อมูลท่ามือของแต่ละตัว
├── 3_train_model.py      ← STEP 3: เทรนโมเดล + ดูว่าตัวไหนสับสนกัน
├── 4_predict_live.py     ← STEP 4: ทายสดจากกล้อง + แสดงข้อความบนจอ
├── app.py                ← STEP 5: หน้าเว็บ Streamlit รวมทุกอย่าง
├── data/                 ← เก็บไฟล์ข้อมูลท่ามือ (สร้างอัตโนมัติ)
└── model.pkl             ← โมเดลที่เทรนเสร็จ (สร้างอัตโนมัติ)
```

## 6. เรื่องการเลือกตัวพยัญชนะ (อ่านก่อนทำ STEP 2)

**หลักการเลือกพยัญชนะที่จะทำ:**
1. ต้องเป็น **ท่านิ่ง** (มือค้างท่าเดียว ไม่ขยับ) — เปิดวิดีโออ้างอิงเช็ค ถ้ามีลูกศร/มือขยับ = ตัดออก
2. ต้อง **ท่าต่างกันชัด** — ถ้า 2 ตัวท่ามือคล้ายกันเกินไป โมเดลจะแยกไม่ออก ให้เลือกมาแค่ตัวเดียวเป็นตัวแทน

**แหล่งวิดีโออ้างอิง:**
- ตารางสะกดนิ้วมือ ม.เกษตร: pirun.ku.ac.th/~fhumalt/THSL/THSL/html/nav_th/THSL_fsp_th.htm
- ฐานข้อมูลภาษามือไทย: th-sl.com

**พยัญชนะตัวตั้งต้นที่แนะนำให้ลอง** (ตัวที่ยืมท่าจากอังกฤษ มักเป็นท่านิ่งเดี่ยว — ให้เช็ควิดีโอยืนยันก่อน แล้วคัดตัวที่ท่าซ้ำกันออก):
`ก ด บ ต ส พ ห ฟ ล ร ว จ ม น อ`
(เลือกมาให้ได้ ~15 ตัวที่นิ่งและต่างกันชัด — ถ้าตัวไหนตอนเทรนแล้วสับสนกับตัวอื่น ค่อยตัดทีหลังได้)

## 7. ลำดับงาน — ทำทีละ STEP (นี่คือส่วนที่สำคัญที่สุด)

### STEP 0 — เตรียมของ
- สร้าง `requirements.txt` ที่มี library ทั้งหมดในข้อ 4
- สร้าง `labels.py` ที่เก็บ list ของตัวที่จะทำ (เริ่มด้วยเลข 0-9 ก่อน ค่อยเพิ่มพยัญชนะทีหลัง)
- อธิบายวิธีติดตั้ง (`pip install -r requirements.txt`)
- **หยุดตรงนี้ ให้ผู้ใช้ติดตั้งและยืนยันว่าสำเร็จก่อน**

### STEP 1 — เปิดกล้อง + จับ 2 มือให้ได้
- สร้าง `1_test_camera.py`
- เปิดกล้อง แสดงภาพสด และวาดจุด 21 จุดบนมือ (ตั้ง max_num_hands = 2 ให้เห็นได้ทั้ง 2 มือ)
- เกณฑ์ว่าเสร็จ: เห็นกล้องทำงาน มีจุดขึ้นบนมือจริง (ลองยก 2 มือก็ขึ้นทั้งคู่) กด `q` เพื่อออก
- ใส่คอมเมนต์ภาษาไทยอธิบายทุกส่วนสำคัญ
- **หยุด ให้ผู้ใช้ทดสอบก่อน**

### STEP 2 — อัดข้อมูลท่ามือ
- สร้าง `2_collect_data.py`
- โปรแกรมถามว่าจะอัดตัวไหน (อ่านจาก labels.py) แล้วให้ผู้ใช้ทำท่าค้างไว้
- **สำคัญ — อัดแบบเพิ่มทีละตัว หลายรอบได้:**
  - เซฟข้อมูลแบบ **"เพิ่มเข้าไป (append)" ไม่ใช่ "ลบของเก่าทิ้ง"** — อัดวันนี้ 3 ตัว พรุ่งนี้มาอัดเพิ่มได้ ของเก่าไม่หาย
  - อัดตัวไหนก็ได้ทีละตัว **ไม่ต้องอัดครบทุกตัวในรอบเดียว**
  - จุดประสงค์: ผู้ใช้อยากลองอัดแค่ 2-3 ตัวก่อน → ไปเทรน (STEP 3) → ลองเล่น (STEP 4) → แล้วค่อยกลับมาอัดตัวที่เหลือเพิ่ม
  - เตือนผู้ใช้ว่า: อัดเพิ่มทุกครั้ง ต้องรัน STEP 3 เทรนใหม่ โมเดลถึงจะรู้จักตัวใหม่
- **ใช้มือเดียว** (มือที่เด่นสุดในเฟรม) บันทึกจุดมือ 63 ตัวเลข ลงไฟล์ในโฟลเดอร์ `data/`
- อัดตัวละหลายๆ ครั้ง (เป้าหมาย ~50-100 ครั้งต่อตัว) เพื่อให้โมเดลแม่น
- แนะนำให้อัดในหลายมุม/หลายระยะเล็กน้อย จะทนทานกว่า
- เกณฑ์ว่าเสร็จ: มีไฟล์ข้อมูลครบทุกตัวใน labels.py
- **หยุด ให้ผู้ใช้อัดข้อมูลจริงก่อน (ชวนเพื่อนในกลุ่มมาอัดหลายคนจะแม่นขึ้น)**

### STEP 3 — เทรนโมเดล + เช็คตัวที่สับสน
- สร้าง `3_train_model.py`
- อ่านข้อมูลทั้งหมดจาก `data/` มาเทรนด้วย RandomForest
- แสดงค่าความแม่น (accuracy) + **แสดง confusion matrix หรือบอกว่าคู่ไหนโมเดลสับสนกันบ่อย**
- ถ้ามีคู่ที่สับสนหนักๆ ให้แนะนำผู้ใช้ว่าควรตัดตัวไหนออก หรืออัดข้อมูลเพิ่ม
- เซฟโมเดลเป็น `model.pkl`
- **หยุด ให้ผู้ใช้เทรนและดูผล**

### STEP 4 — ทายสด + แสดงข้อความบนจอ
- สร้าง `4_predict_live.py`
- เปิดกล้อง → จับมือ (ใช้มือเดียวเด่นสุด) → โหลด `model.pkl` มาทาย
- แสดงตัวที่ทายได้เป็น "ข้อความตัวใหญ่" บนจอกล้อง (ไม่ต้องมีเสียง)
- กันไม่ให้ข้อความกระพริบถี่เกินไป (เปลี่ยนเมื่อผลนิ่งพอ หรือหน่วงเวลานิดหน่อย)
- **(ออปชั่นเสริม ถ้าเวลาเหลือ)** โหมด "สะกดต่อกัน": เก็บตัวที่ทายได้เรียงกันเป็นข้อความ แสดงเป็นบรรทัดข้อความบนจอ
- **หยุด ให้ผู้ใช้ทดสอบ**

### STEP 5 — ทำเป็นเว็บด้วย Streamlit
- สร้าง `app.py`
- หน้าเว็บมีปุ่มเปิดกล้อง แสดงภาพสด + ตัวที่ทายได้เป็นข้อความตัวใหญ่
- ทำ UI ให้เรียบง่าย สวยพอสำหรับ demo (มีชื่อ HandSpeak, คำอธิบายสั้นๆ)
- **หยุด ให้ผู้ใช้ทดสอบ demo เต็มรูปแบบ**

## 8. กฎการทำงานสำหรับ Claude Code

1. ทำ **ทีละ STEP** จบแต่ละ STEP ให้หยุดรอผู้ใช้ทดสอบและยืนยันก่อนไปต่อ
2. โค้ดต้องมี **คอมเมนต์ภาษาไทย** อธิบายส่วนสำคัญ (เจ้าของต้องเข้าใจเพื่อตอบ demo)
3. ใช้โครงสร้างโค้ดที่ **มือใหม่เข้าใจได้** เลี่ยงเทคนิคซับซ้อนเกินจำเป็น
4. ถ้ามีทางเลือกระหว่าง "เท่แต่ยาก" กับ "ธรรมดาแต่เข้าใจง่าย" → เลือกอันหลังเสมอ
5. หลังเขียนโค้ดแต่ละไฟล์ ให้ **อธิบายสั้นๆ ว่าไฟล์นี้ทำอะไร** เป็นภาษาไทย
6. เริ่มทำสโคปเล็กก่อน (เลข 0-9) ให้ทั้งระบบวิ่งครบวงจร แล้วค่อยเพิ่มพยัญชนะ
7. ออกแบบให้ทำงาน **เป็นรอบ (iterative)** ได้ — ผู้ใช้จะอัดข้อมูลนิดเดียว → เทรน → ทดสอบ → กลับมาอัดเพิ่ม → เทรนใหม่ วนไปเรื่อยๆ ข้อมูลเก่าต้องไม่หายเมื่ออัดเพิ่ม และเทรนใหม่ได้เสมอ

## 9. Prompt ตั้งต้น (ก๊อปอันนี้ไปวางใน Claude Code)

```
อ่านไฟล์ brief.md ในโฟลเดอร์นี้ให้ครบก่อน แล้วสรุปให้ฉันฟังสั้นๆ
ว่าเข้าใจโปรเจคนี้ว่ายังไง และจะทำ STEP 1 ยังไง
อย่าเพิ่งเขียนโค้ด รอฉันบอก "โอเคเริ่มเลย" ก่อน
```
